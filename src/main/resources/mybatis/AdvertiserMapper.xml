<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.duode.mapper.CardMapper">
 
    <select id="findAdvertiser" resultType="com.duode.model.Advertiser">
      select * from advertiser where id=#{id}
    </select>

    <insert id="addAdvertiser">
    insert into advertiser(
        `name`,
        comment,
        address,
        status,
        telephone,
        create_time,
        modify_time
    )
    values (
        #{advertiser.name},
        #{advertiser.comment},
        #{advertiser.address},
        #{advertiser.status},
        #{advertiser.telephone},
        now(),
        now()
    )
    </insert>
</mapper>